# 工作准则

1. 默认只修改当前工作路径代码，不允许编辑其他工作路径。除非我显示的要求你将仓库的最新修改生效到本机，才允许进行其他修改。
2. 每次新会话都需要确认本地版本是否最新，不是最新则向用户确认是否需要更新。
3. skill 放在 `.claude/skills/` 目录下即可被 Claude Code 自动发现，不需要在 `.claude/commands/` 中创建额外的入口文件。commands 和 skills 是独立机制，不要混淆。
